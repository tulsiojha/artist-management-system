"use client";
import { cn } from "@/utils/commons";

const Header = () => {
  return (
    <nav className="border-b-1 border-gray-200 h-[70px] flex items-center px-2">
      <div className="font-bold">Welcome,</div>
    </nav>
  );
};

export default Header;
